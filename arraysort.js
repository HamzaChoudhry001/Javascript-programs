// // sort()
// //reverse() 
// // The reverse() method reverses the elements in an array.

// // You can use it to sort an array in descending order:
// const array = ["G","C","A","B"];

// console.log(array.reverse());
// console.log(array.sort());

// Numeric Sort
// let points = [40, 100, 1, 5, 25, 10];
// let z=points.sort(function(a, b){return a - b});

//  console.log(z); //1, 5, 10, 25, 40, 100 
// console.log(z[0]);//1 lowest value
// function arr(arr1){
//     return Math.max.apply(null, arr1);
// }
// console.log(arr(points));//100 highest

// Sorting Object Arrays
var obj = [{name: "hamza", age:21},{name: "ali", age:31},{name: "ajmed", age:5},]
// console.log(obj.sort(function arr(a,b){
//     return a.age-b.age;
// }));

// Comparing string properties is a little more complex:
